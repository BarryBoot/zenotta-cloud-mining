apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name:  {{ include "miner.fullname" . }}-pvc
spec:
  volumeName:  {{ include "miner.fullname" . }}-pv
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: zenotta-wallet